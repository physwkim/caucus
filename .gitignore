/target
**/*.rs.bk
Cargo.lock.bak

# caucus runtime state — never tracked
.caucus/

# Claude Code per-project state — never tracked
.claude/

# editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# kodex (code-graph MCP) local cache
kodex-out/
